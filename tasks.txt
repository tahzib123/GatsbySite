[x] Gatsby Images
[x] Gatsby SEO
[x] PWA with gatsby-plugin-offline ??? maybe not possible when using react window
[x] Category filtering
[x] remove api keys from gatsby config into env variables    https://codesandbox.io/s/github/smakosh/gatsby-portfolio-dev?file=/gatsby-config.js
[x] 404 page  
[x] better active category effect
[x] search bar should not be case sensitive
[x] Fix card container to make images take proper height of container
[] google analytics https://www.gatsbyjs.org/docs/adding-analytics/
[] contact form https://www.gatsbyjs.org/docs/building-a-contact-form/
[] google featured snipits 
[] add keys for props
[] source own fonts https://markoskon.com/extremely-fast-load-time-with-gatsby-and-self-hosted-fonts/
[] new color font for link items and category lists
[] AMP
[] scroll to top button
[] remove all console logs
[] categorys should be made programmatically

[] if applying both filters then it should narrow down the product array
[] clear filters should reset back to the category not all the products (store currentCategory array and use it for filters. Then when clear filters is clicked reset state to currentCategory array)
[] fix image border radius
[]when you search for an item clear the active category


[x] search bar


                {categories.map((value, index) => {
                    return <li className = {categoryStyles.listItems} key={index}><button className = {categoryStyles.categoryButtons} id = {activeIndex === index ? categoryStyles.active : categoryStyles.inactive} onClick = {() => onCategoryChange(value, 0)}>{value}</button></li>
                })}
